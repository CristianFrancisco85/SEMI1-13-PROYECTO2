import PublicationView from "./publicationView"

const Publication = (props) =>{

    const {data} = props
    
    return(
        <PublicationView>
        </PublicationView>
    )
   
}
export default Publication